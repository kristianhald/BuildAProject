<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <add key="repositorypath" value="TheSecondFile" />
  </config>
  <packageRestore>
    <add key="enabled" value="True" />
    <add key="automatic" value="True" />
  </packageRestore>
  <packageSources>
    <add key="Test" value="AdditionalPackageSource" />
  </packageSources>
  <disabledPackageSources>
    <clear /> <!-- ensure only the sources defined below are used -->
  </disabledPackageSources>
  <activePackageSource>
    <clear /> <!-- ensure only the sources defined below are used -->
  </activePackageSource>
</configuration>